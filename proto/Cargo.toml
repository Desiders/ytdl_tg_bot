[package]
name = "ytdl_tg_bot_proto"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = { version = "0.12", features = ["codegen", "prost", "transport"], default-features = false }
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
