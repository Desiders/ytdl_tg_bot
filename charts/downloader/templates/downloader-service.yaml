apiVersion: v1
kind: Service
metadata:
  name: downloader
  namespace: {{ .Release.Namespace }}
spec:
  clusterIP: None
  selector:
    app: downloader
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
