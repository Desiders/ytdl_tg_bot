[workspace]
members = [
    "bot",
    "downloader",
    "proto",
]
exclude = ["migration"]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1

[profile.k8s-dev]
inherits = "dev"
opt-level = 1
incremental = true
codegen-units = 256
debug = 0
lto = "off"
