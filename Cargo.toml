[workspace]
members = [
    "bot",
    "cookie_assignment",
    "downloader_client",
    "downloader",
    "proto",
    "migration",
]
resolver = "2"

[profile.release]
lto = "thin"
codegen-units = 1
